golang {
    packagePath = 'game' // go import path of project to be built, NOT local file system path!
    goVersion = '1.8.1'
}

dependencies {
    golang {
        build name:'github.com/xtaci/kcp-go'
    }
}
